set(VCPKG_LIBRARY_LINKAGE static)

if(PORT STREQUAL "opencv4")
  set(VCPKG_LIBRARY_LINKAGE dynamic)
  set(VCPKG_CMAKE_CONFIGURE_OPTIONS -DWITH_V4L=OFF -DCMAKE_SHARED_LIBRARY_SUFFIX_CXX=_maa.so)
endif()
