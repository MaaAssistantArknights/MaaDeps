diff --git a/cmake/onnxruntime_common.cmake b/cmake/onnxruntime_common.cmake
index aa9dfcf..0581164 100644
--- a/cmake/onnxruntime_common.cmake
+++ b/cmake/onnxruntime_common.cmake
@@ -72,6 +72,9 @@ if(onnxruntime_target_platform STREQUAL "ARM64EC")
 endif()
 
 if(onnxruntime_target_platform STREQUAL "ARM64")
+    if (CMAKE_CXX_COMPILER_ID STREQUAL "Clang" AND CMAKE_SYSTEM_NAME STREQUAL "Linux")
+        set(ARM64 TRUE)
+    endif ()
     if (MSVC)
         add_compile_options("/bigobj")
     endif()
@@ -184,6 +187,11 @@ elseif(NOT CMAKE_SYSTEM_NAME STREQUAL "Emscripten")
       set(X86 TRUE)
     endif()
   else()
+    if (CMAKE_CXX_COMPILER_ID STREQUAL "Clang" AND CMAKE_SYSTEM_NAME STREQUAL "Linux")
+      if (NOT ARM64)
+        set(X86_64 TRUE)
+      endif ()
+    else ()
     execute_process(
       COMMAND ${CMAKE_C_COMPILER} -dumpmachine
       OUTPUT_VARIABLE dumpmachine_output
@@ -202,6 +210,7 @@ elseif(NOT CMAKE_SYSTEM_NAME STREQUAL "Emscripten")
     elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "^(x86_64|amd64)$")
       set(X86_64 TRUE)
     endif()
+    endif ()
   endif()
 endif()
 
