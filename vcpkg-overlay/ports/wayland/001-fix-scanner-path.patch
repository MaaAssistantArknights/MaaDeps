diff --git a/meson_options.txt b/meson_options.txt
index b8e2ec6..1bbb452 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -22,3 +22,7 @@ option('icon_directory',
   description: 'Location used to look for cursors (defaults to ${datadir}/icons if unset)',
   type: 'string',
   value: '')
+option('scanner_path',
+  description: 'Scanner path',
+  type: 'string',
+  value: '')
diff --git a/src/meson.build b/src/meson.build
index a8a1d2b..1e8b770 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -78,8 +78,7 @@ if get_option('scanner')
 endif
 
 if meson.is_cross_build() or not get_option('scanner')
-	scanner_dep = dependency('wayland-scanner', native: true, version: meson.project_version())
-	wayland_scanner_for_build = find_program(scanner_dep.get_variable(pkgconfig: 'wayland_scanner'))
+	wayland_scanner_for_build = find_program('wayland-scanner', get_option('scanner_path'))
 else
 	wayland_scanner_for_build = wayland_scanner
 endif
